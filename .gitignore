node_modules/
dist/
dist-server/
*.tsbuildinfo
vite.config.js
vite.config.d.ts
.playwright-cli/
output/playwright/
.worktrees/
test-results/
playwright-report/
coverage/
.env
.env.*

# Markdown docs — only README.md and CLAUDE.md are kept in the repo so a fresh
# clone carries the project's conventions/invariants. (AGENTS.md is left
# disk-only: it predates the content-registry refactor and the LAN ship.)
*.md
!README.md
!CLAUDE.md

# AI agent / model config files and directories
.codex
.claude/
.github/instructions/
.github/prompts/
.github/agents/
.github/hooks/
docs/

# Playwright MCP session artifacts
.playwright-mcp/

# Loose screenshot artifacts
*.png
*.log
